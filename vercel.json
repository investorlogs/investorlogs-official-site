{
  "framework": "nextjs",
  "buildCommand": "prisma generate && (prisma migrate deploy || echo 'WARN: prisma migrate deploy skipped (no DATABASE_URL)') && next build",
  "crons": [
    {
      "path": "/api/cron/smm-status",
      "schedule": "*/5 *"
    },
    {
      "path": "/api/cron/supplier-sync",
      "schedule": "0 */6 *"
    }
  ]
}